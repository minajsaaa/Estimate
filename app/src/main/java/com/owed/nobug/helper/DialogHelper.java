package com.owed.nobug.helper;

public class DialogHelper {
/*
    public static void showMaintenance(final Context mContext, VersionItem item) {
        String title = mContext.getString(R.string.maintenance_title);
        String message = mContext.getString(R.string.maintenance_message);

        AlertUtil.alertPositive(mContext, title, message, null, false,
                new DialogInterface.OnClickListener() {
                    @Override
                    public void onClick(DialogInterface dialog, int which) {
                        ((Activity) mContext).finish();
                    }
                });
    }

    public static void showVersionUpdate(final Context mContext, final VersionItem item, DialogInterface.OnClickListener handler) {
        String title = mContext.getString(R.string.version_update);
        String message = mContext.getString(R.string.need_app_update_desc);

        AlertUtil.alertPositive(mContext, title, message, null, true, handler);
    }

    public static void showRetry(final Context context, DialogInterface.OnClickListener handler) {
        String message = context.getString(R.string.network_error_status);
        String confirm = context.getString(R.string.retry);
        AlertUtil.alertPositive(context, null, message, confirm, true, handler);
    }*/

}
